import { Image, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const User = ({ user, setShow }) => {
  return;
};
const styles = StyleSheet.create({});

export default User;
